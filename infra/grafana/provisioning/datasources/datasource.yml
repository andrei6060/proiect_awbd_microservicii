apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Grafana reaches Prometheus over the shared docker network by service name.
    url: http://prometheus:9090
    isDefault: true
    editable: true
